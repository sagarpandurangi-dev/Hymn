# Isolated pytest config for the network-free safety suite. Placing this
# file here prevents pytest from walking upward and picking up
# `backend/conftest.py` (whose integration-test guard is not relevant
# for these self-contained unit tests):
#     pytest backend/safety_tests
[pytest]
minversion = 7.0
testpaths = .
addopts = -q
